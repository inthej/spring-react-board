<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.board.backend.mapper.BoardCommentMapper">

    <select id="selectBoardComments" resultType="com.board.backend.model.BoardCommentDto$ListItem">
        SELECT t.*
        FROM (
            SELECT
                ROW_NUMBER() OVER (ORDER BY id) as rownum,
                board_reply.*
            FROM board_reply
            WHERE bid = #{bid}
        ) AS t
    </select>

    <select id="selectBoardCommentById" resultType="com.board.backend.model.BoardCommentDto$Response">
        SELECT id,
               bid,
               content,
               writer,
               created_id,
               created_dt,
               modified_dt
        FROM board_reply
        WHERE id = #{id}
    </select>

    <select id="selectBoardCommentDetailById" resultType="com.board.backend.model.BoardCommentDto$Detail">
        SELECT id,
               bid,
               content,
               writer,
               password,
               created_id,
               created_dt,
               modified_dt
        FROM board_reply
        WHERE id = #{id}
    </select>

    <insert id="insertBoardComment" parameterType="com.board.backend.model.BoardCommentDto$Create" keyProperty="form.id" useGeneratedKeys="true">
        INSERT INTO board_reply (bid, content, writer, password, created_id)
        VALUES (#{bid}, #{form.content}, #{form.writer}, #{form.password}, #{form.created_id})
    </insert>

    <update id="updateBoardCommentById" parameterType="com.board.backend.model.BoardCommentDto$Update">
        UPDATE board_reply
        SET content=#{form.content},
            writer=#{form.writer},
            password=#{form.password}
        WHERE id = #{id}
    </update>

    <delete id="deleteBoardCommentById" parameterType="long">
        DELETE FROM board_reply
        WHERE id = #{id}
    </delete>

    <delete id="deleteAllByBoardId" parameterType="long">
        DELETE FROM board_reply
        WHERE bid = #{bid}
    </delete>
</mapper>